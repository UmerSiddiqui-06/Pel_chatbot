# Leave unset to run against the in-memory mock chat service.
# Once a backend exists, copy this file to .env and set the real URL:
# VITE_API_BASE_URL=https://api.pel-ai.example.com
VITE_API_BASE_URL=
